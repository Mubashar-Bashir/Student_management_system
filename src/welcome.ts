// Import necessary modules and classes
import { ChalkInstance } from "chalk";
import chalk from "chalk";


const welcomeMsg = chalk.bgCyan.white(`\t\tWelcome to PIAIC Student Management System`);
const line = "=".repeat(welcomeMsg.length);

export {line,welcomeMsg} ;